import { ConfigInterface } from '~/lib/types/general.types';

const config: ConfigInterface = {
  APP_URL: 'https://poa-app.demo.apillon.io',
  API_BASE: 'https://poa-api.demo.apillon.io',
};

export default config;
